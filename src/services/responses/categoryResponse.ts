export interface CategorySearchResponse {
  id: string;
  status?: string;
  name: string;
  createdOn?: string;
  modifiedOn?: string;
}

export interface CategoryListSelectItem {
  id: string;
  name: string;
}
